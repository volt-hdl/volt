// This file was generated by Volt. DO NOT EDIT.
#include "VUartTx.h"
#include "verilated.h"
#include <cstdio>

using TOP = VUartTx;

static void run_cycle(TOP* dut, VerilatedContext* ctx) {
    dut->clk = 1;
    dut->eval();
    ctx->timeInc(1);
    dut->clk = 0;
    dut->eval();
    ctx->timeInc(1);
}

static void apply_reset(TOP* dut, VerilatedContext* ctx) {
    dut->rst = 1;
    run_cycle(dut, ctx);
    run_cycle(dut, ctx);
    dut->rst = 0;
}

static bool test_0() {
    VerilatedContext uctx;
    VerilatedContext* ctx = &uctx;
    TOP dut(ctx);
    dut.start = 0;
    dut.data = 0;
    apply_reset(&dut, ctx);
    for (unsigned long long s = 0; s < 1ULL; ++s) run_cycle(&dut, ctx);
    if (!(((unsigned long long)dut.tx) == (0ULL))) {
        std::printf("VOLT-ASSERT-FAIL assert_eq uart_tx2_test.volt:4 left=%llu right=%llu\n", (unsigned long long)dut.tx, 0ULL);
        dut.final();
        return false;
    }
    dut.final();
    return true;
}

int main(int, char**) {
    int failed = 0;
    std::printf("VOLT-TEST-BEGIN deliberately_wrong\n");
    if (test_0()) std::printf("VOLT-TEST-END deliberately_wrong ok\n");
    else { ++failed; std::printf("VOLT-TEST-END deliberately_wrong fail\n"); }
    return failed == 0 ? 0 : 1;
}
