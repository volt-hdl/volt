[package]
name = "volt-syntax"
version.workspace = true
edition.workspace = true
license.workspace = true
# benches/synthetic.rs paylaşılan yardımcı modüldür, kendi başına bench hedefi değildir.
autobenches = false

[dependencies]
volt-span = { path = "../volt-span" }
volt-diagnostics = { path = "../volt-diagnostics" }
volt-ast = { path = "../volt-ast" }
logos.workspace = true

[dev-dependencies]
criterion.workspace = true

[[bench]]
name = "lexer_bench"
harness = false

[[bench]]
name = "parser_bench"
harness = false
